"""
app - where core meets integrations, plus the commands you actually run.

live_agent.py feeds real Swiggy data into core's unmodified planner,
execute_live.py pushes an approved plan into a real cart, and the
run_*.py files are the entry points a human types.
"""
